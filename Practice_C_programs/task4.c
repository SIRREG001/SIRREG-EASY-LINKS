#include <stdio.h>

/**
 * This program finds the product of 29 and 5
 * A basic arithmetic operation
 */
int main(void)
{
	int a, b, product;
	
	a = 29;
	b = 5;
	product = a * b;
	printf("The product of %d and %d is %d\n", a, b, a * b);
}

#include <stdio.h>

/**
 * This program prints the sum of 29 and 5
 * To declare variables in C and assign values to them
 */

int main(void)
{
	int a, b, sum;
	a = 29;
	b = 5;
	sum = a + b;
	printf("%d + %d = %d\n", a, b, sum);
}
